/*#include "computador.h"
#include "fila.h"
#include "rede.h"
#include "listaComp.h"*/

/*int escalonador_fcfs(char *s);
int escalonador_sjf(char *s);
void fila_cpu_FIFO(Comp *c, int n);
void fila_disco_FIFO(Comp *c, int n);
Fila* fila_rede_FIFO(Comp *c, int n);
ListaComp* cria_computadores(ListaComp *l, int num_comp, Rede *r);
void taxas_processamento(Fila *f);
void libera_memoria_neolook(ListaComp *l, Fila *f);
void falha();
void neolook_escalonador(ListaComp *lc, Comp *c, int num_comp, char *escalonador, Fila *f1);
*/
void neolook(char *text, char *escalonador, int num_comp);
